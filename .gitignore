__pycache__/
*.pyc
grants.db
grants.db-wal
grants.db-shm
.venv/

# PyInstaller build/output - these bundle a full copy of grants.db
# (real scholar names, ages, etc.) plus logs, so they must never be
# committed even though grants.db itself already is.
auth.txt
users.json
users.json.lock
build/
dist/
logs/
*.log
.venv/
.vs/
payroll_inputs/
payroll_outputs/
backups/
*.db.backup*
.vscode/
network.txt
.coverage
# Local payroll database runtime files
payroll.db
payroll.db-wal
payroll.db-shm

# Diagnostic backups such as grants.db.pre-delete-diagnostic.backup
*.db.*.backup*
